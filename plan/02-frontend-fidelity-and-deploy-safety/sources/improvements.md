# Improvements & deferred ideas

One line per entry, most recent first.

- Phase 11 (2026-09-16): [note] Enabling `renderer.shadowMap.enabled` (temporarily, to evaluate Phase 6/Phase 11's dead shadow config on `World_Environment.js`) visibly darkened/discolored the display stand on `/de/projects/isphording-inneneinrichtung` (neutral gray → muddy bronze) via self-shadowing, with no floor/ground mesh to justify a contact shadow — judged not a clear improvement, so the dead `castShadow`/`receiveShadow`/`shadow.mapSize`/`shadow.normalBias` config was removed instead of wiring up `shadowMap.enabled`. Before/after/rejected screenshots saved to `plan/02-frontend-fidelity-and-deploy-safety/evidence/phase-11/`.
- Phase 11 (2026-09-16): [note] `ImageZoom.mjs`'s NDC-from-canvas-rect fix and the AbortController-based lightbox listener fix could only be verified by code inspection, not in a browser — same pre-existing gap as Phase 5/6/10's findings: the `moodboard` template has no content page anywhere in `app/content`, so it's unreachable via any URL.

- Phase 8 (2026-09-16): [note] No content page uses the `project.three` template (`grep -rl "project.three" app/content/` returns nothing), so the WebGL-experience acceptance criterion for this phase was verified statically — `app.bundle.js` inlines `runExperience`'s `dataset.world`/`#webgl` logic and imports the vendor-three chunk correctly after the dead entry removal — rather than in a real browser; wire up a project using that template if live QA of the Three.js path is ever needed.
- Phase 4 (2026-09-16): [idea] With the outgoing container removed at the end of `leave()`, there is a ~600ms window mid-transition where the Barba wrapper holds *zero* containers — the gap between the removal and Barba appending the network-fetched next container. It is fully hidden by the fixed `.loadingScreen` today, so no flash; but Phase 5's readiness-gate rework must keep the reveal (`animateLoaderOut`) from starting during that gap, not just after the container count returns to 1. Verified via chrome-devtools MCP (container-count timeline `1…0…1`).
- Phase 4 (2026-09-16): [idea] Browser QA of the Barba transition needs the Vite dev server running on :9001 in addition to a `url`-matched PHP host — in dev mode (`config.localhost.php` sets `vite.server`) the app bundle is served from :9001, so with it down `animBarba()` never runs, the site does a full reload on every link, and `smoke.sh` still passes green (it only asserts HTTP 200 + clean markup). Compounds the already-logged `config.localhost.php` url:8000 workaround; a request-derived `url` plus a smoke assertion that the bundle actually loads would close both gaps.

- Phase 3 (2026-09-16): [bug] `app/site/snippets/project-gallery.php`'s custom-video branch renders one `<video>` per matching *image file*, not per base name — when a case-study video's base name has both a `.jpg` and a `.webp` sibling (as `isphinnen_20_30_project_casestudy_iPhone` and `isphinnen_20_50_project_casestudy_iPad` do), the loop emits the same video twice, doubling network requests. Confirmed with chrome-devtools MCP on `/de/projects/isphording-inneneinrichtung` (4 `<video>` network requests for 2 distinct clips). Pre-existing, not introduced by this phase's URL fix; dedupe by base name like the image-grouping loop below it already does.
- Phase 3 (2026-09-16): [cleanup] Resolved the two Phase 5/2 findings below: `project.three.php` now passes `'videoPath' => 'video/'` (webroot-relative) instead of `'app/video/'`, `project-gallery.php` builds URLs with Kirby's `url()` helper and resolves `file_exists()` against `kirby()->root('index')`, and `media-processing/index.php`'s `file.create:after` hook now calls `$file->mediaDir()` (works for Page/Site/User parents alike) instead of hand-building `'pages/' . $file->parent()->diruri()`.
- Phase 6 (2026-09-16): [bug] `scripts/deploy.sh:70` mirrors `app/` with `--delete` but doesn't exclude `/assets/three/`, which is gitignored. A deploy from a clone that lacks those files would delete the live glTF model, env map and Draco decoder. Phase 6 mirrored them locally, so decide between excluding `/assets/three/` or versioning the assets.
- Phase 6 (2026-09-16): [cleanup] The two Phase 5 World_01 entries below (empty canvas, missing `/assets/three` assets) are resolved: `World.mjs` now calls `resources.loadOtherResources()`, and the assets were mirrored from live into gitignored `app/assets/three/`.
- Phase 6 (2026-09-16): [idea] `Resources` has an implicit start protocol: each world must remember to call `loadOtherResources()` or `startProgressiveLoading()`. A single `resources.start()` chosen per world would stop this regressing again. Area: `dev/js/three/utils/Resources.mjs`.
- Phase 6 (2026-09-16): [bug] `dev/js/three/projects/moodboard/ImageZoom.mjs:70-71` computes mouse NDC from `window.innerWidth/innerHeight` instead of the canvas rect, so hover and click hit-testing drift when the canvas isn't full-window. Its lightbox also adds a `keydown` listener that is never removed, so a later Escape calls `removeChild` on a detached overlay.
- Phase 6 (2026-09-16): [cleanup] `World_Environment.js` sets `castShadow`, `receiveShadow` and the shadow `mapSize`, but `renderer.shadowMap.enabled` is never set, so the shadow setup is dead code. Either enable shadows (and re-check visual parity) or remove it.
- Phase 6 (2026-09-16): [idea] The live Draco decoder (`/assets/three/libs/draco/`) differs from the copy shipped in `node_modules/three/examples/jsm/libs/draco/`. Copying it from `node_modules` at build time would keep decoder and `DRACOLoader` versions in lockstep.
- Phase 6 (2026-09-16): [idea] Keep the before/after WebGL capture harness (a `capture.mjs`/`compare.mjs` pair built during the Kirby 5 recovery plan's Three.js phase, which currently imports `puppeteer-core` from an npx cache path) as a proper devDependency script for future three.js upgrades.

- Phase 5 (2026-09-16): [bug] The `project.three` WebGL scene (e.g. `/de/projects/isphording-inneneinrichtung`) renders an empty canvas: `dev/js/three/projects/isphording-inneneinrichtung/World.mjs` only listens for `resources.on('resourcesReady', …)` but nothing ever calls `resources.startProgressiveLoading()` for this project — only `dev/js/three/projects/moodboard/ProgressiveLoader.mjs:42` calls it, for the moodboard project. Zero network requests for the model/textures are made, `resourcesReady` never fires, and `DisplayAppleXDR` is never added to the scene. Verified with chrome-devtools MCP: canvas exists (2576×1288) but `readPixels` returns `[0,0,0,0]` at center. Likely a side effect of the recent moodboard progressive/batch-loading refactor (see git log `6cf4ece`…`ea6c3a8`); in scope for Phase 6 (Three.js upgrade + rendering regressions), not this phase.
- Phase 5 (2026-09-16): [bug] Even if loading were triggered, it would still 404: `dev/js/three/projects/isphording-inneneinrichtung/World_Sources.mjs` requests `/assets/three/textures/environmentMaps/0/*.jpg` and `/assets/three/meshes/gltf/AppleDisplayXDRSingle.glb`, and `dev/js/three/utils/Resources.mjs` points the Draco decoder at `/assets/three/libs/draco/` — none of `/assets/three/{textures,meshes,libs}` exist anywhere in the repo (checked `app/assets`, `dev/assets`, full repo search). These binary assets were apparently never committed.
- Phase 5 (2026-09-16): [bug] On `project.three` pages, the case-study video inserts (iPhone/iPad screens) 404: `app/site/templates/project.three.php` passes `'videoPath' => 'app/video/'` to `project-gallery.php`, which builds URLs like `http://<host>/app/video/isphinnen_20_30_project_casestudy_iPhone.mp4` — a path that doesn't correspond to any real Kirby media/content URL (no `/app/video/` route exists). The rest of the gallery falls back to real Kirby file URLs and works fine.
- Phase 5 (2026-09-16): [idea] The `moodboard` template (`app/site/templates/moodboard.php`, canvas-minimal snippet with pan/zoom moodboard experience) has no content page anywhere in `app/content` (`$kirby->page('moodboard')` returns null) — confirmed via a one-off PHP script against the Kirby app. It cannot be reached or verified via any URL today; if it's meant to be live, it needs a content page, or it should be documented as work-in-progress/removed from the site index.
- Phase 5 (2026-09-16): [idea] `config.localhost.php`'s hardcoded `'url' => 'http://localhost:8000'` breaks CSS/media loading in the browser whenever the PHP dev server runs on a different port (needed on machines where port 8000 is already held by an unrelated local service, as documented in Phase 2's findings and this machine's `PORT=8011` convention) — verified with chrome-devtools MCP: `app.css` and all project thumbnails came back `net::ERR_BLOCKED_BY_ORB` because the HTML requested them from `http://localhost:8000/...` while the site was actually served on `127.0.0.1:8011`. Not fixed here (would need either a dynamic host/port detection or documenting "always free port 8000 for browser QA"); noted in `readme/QUICK_START.md`.
- Phase 5 (2026-09-16): [idea] Four licensed font files referenced by `dev/css/templates/_typography.scss` (`RadioGrotesk-Regular.otf`, `RadioGrotesk-Bold.otf`, `MonumentExtended-Bold.otf`, `Grafier-Regular.otf`) don't exist anywhere in the repo, so every page 404s on them in the browser (harmless — each `@font-face` has a `local(...)` fallback and the CSS font-stacks degrade to system fonts) — these are presumably paid/licensed fonts intentionally excluded from git; if design fidelity matters for local QA, someone needs to drop the `.otf` files into `dev/assets/fonts` (or wherever the build expects them) by hand.
- Phase 4 (2026-09-16): [idea] `vite.config.build.js` declares a second rollup entry `three: dev/js/three/runExperience.js`, but `dev/js/index.js` already statically imports `runExperience.js` and nothing in app/site references `three.bundle.js` directly — the `three` entry looks redundant and duplicates output; check whether it can be dropped.
- Phase 4 (2026-09-16): [idea] `config.php`'s production pages cache is left off (comment explains why: `languages.detect` redirects + risk of serving one visitor's cached redirect to another) — a later phase could investigate a cache strategy safe with language detection instead of leaving it off indefinitely.
- Phase 3 (2026-09-15): [idea] `npm run dev:assets-only` (`vite.assets-only.config.js`) is unreferenced by any code, test, or CI, and `readme/QUICK_START.md:72` is its only mention; it starts fine under Vite 8 but is a maintenance burden — consider deleting it and the doc reference in a later cleanup phase.
- Phase 3 (2026-09-15): [idea] All three vite configs (`vite.config.build.js`, `vite.config.dev.js`, `vite.assets-only.config.js`) print a `configLoader: 'native'` CJS-syntax warning on every run because they use ESM `import`/`export` without `"type": "module"` in `package.json`; harmless today but will need a `.mjs` rename or a package.json `type` field before a future Vite major makes native config loading the default.
- Phase 3 (2026-09-15): [cleanup] `readme/VITE_OPTIMIZATION_SUMMARY.md` documents a `manualChunks` shape (`three-modules`, `three-world`, `utils` chunks) that no longer matches `vite.config.build.js` (now just `vendor-three` via a function-form `manualChunks` for Rolldown compatibility) — the doc was already stale before this phase and could use a rewrite.

- Phase 2 (2026-09-15): [bug] `app/kirby/` is gitignored but still tracked (3,294 Kirby 4.8.0 files in HEAD since `546dace`); the 5.5.3 upgrade shows as thousands of unstaged changes — decide between committing the core or `git rm -r --cached app/kirby` + `composer install` on deploy.
- Phase 2 (2026-09-15): [bug] `scripts/smoke.sh` never checks that its own `php -S` bound the port, so an occupied port (a local uvicorn holds 8000) makes it silently test the wrong server — fail fast on bind error.
- Phase 2 (2026-09-15): [cleanup] `app/site/templates/home.php:21` links the showreel files directly under `/content/<diruri>/`, which Kirby's standard `.htaccess` blocks — resolve them as Kirby files instead.
- Phase 2 (2026-09-15): [bug] `app/site/plugins/media-processing/index.php:11` calls `$file->parent()->diruri()`, which fatals for site-level files (parent is `Site`) on Panel upload.
- Phase 2 (2026-09-15): [idea] On-demand GD thumbs work under Kirby 5.5, so the Kirby-4.8 workaround `utils/build-media-cache.php` (`npm run build:media`) and `readme/MEDIA_CACHE_README.md` may be removable.
- Phase 2 (2026-09-15): [idea] Kirby's "Change the PHP version" page is served with HTTP 200 (the live site is currently down this way), so an uptime check on status code alone would miss it — monitor for the body text too.

- Phase 1 (2026-09-15): `readme/QUICK_START.md` claims `npm run dev` auto-starts both the PHP server and Vite and references a nonexistent `npm run dev:full` script; this pre-dates Phase 1 and wasn't touched beyond adding notes on `npm run php` and `scripts/smoke.sh` — the rest of the doc (XAMPP/Windows setup) is stale and could use a full rewrite in a later phase.
- Phase 1 (2026-09-15): Live server's CLI `php -v` over ssh reports PHP 4.4.9 (cgi-fcgi), completely unrelated to the actual web PHP version (8.4, confirmed via the IONOS control panel). Any future automation that shells out to check the live PHP version via ssh should not trust `php -v` on this host.

- Phase 1 (2026-09-16): [bug] Confirmed the Phase-5 finding about `config.localhost.php`'s hardcoded `'url' => 'http://localhost:8000'` still blocks local browser QA of any `url()`-built asset link (fonts included) whenever the dev server runs on a different port — this machine's port 8000 is genuinely held by an unrelated `python3.1`/uvicorn process, so `php -S 127.0.0.1:8000` silently fails to bind. Verified the font fix by temporarily pointing `config.127.0.0.1.php` at `http://127.0.0.1:8011` and reverting after; a permanent fix (env-based or request-derived `url`) would remove the need for that workaround.
- Phase 1 (2026-09-16): [idea] `Monument-Extended` weight 900 (`MonumentExtended-Black`) is declared in `dev/css/templates/_typography.scss` but no selector anywhere applies `font-weight: 900` to Monument-Extended-styled content, so the browser never fetches that face under normal use (confirmed via `document.fonts` on `/de/`: only the four faces actually rendered on the page show `status: 'loaded'`; the file itself loads fine when fetched directly with the FontFace API). Either wire up a 900-weight use case or drop the unused face.
- Phase 6 (2026-09-16): [note] Phase 6's spec called for asserting `app/kirby/index.php` exists post-Composer-install, but getkirby/cms's actual entry point is `app/kirby/bootstrap.php` — there is no top-level `index.php` in the core package; `deploy.sh`'s new assertion checks `bootstrap.php` instead.

- Phase 10 (2026-09-16): [note] The Draco decoder is now a build artifact (`scripts/copy-draco.mjs`, wired as `prebuild`) mirrored from `node_modules/three` into `app/assets/three/libs/draco/`; when Phase 7 adds an `/assets/` rsync exclude it MUST keep `/assets/three/libs/draco/` in the push or production will never receive the build-generated decoder (reconciliation noted in `scripts/deploy.sh` build comment).
- Phase 10 (2026-09-16): [note] The moodboard world (`World_02`) has no reachable URL (`app/content/projects/` has no moodboard page), so its `resources.start({...})` change was verified only by code inspection, not in a browser; the isphording world was browser-verified (non-empty canvas, model/env/Draco requests 200, no Draco console errors).
- Phase 10 (2026-09-16): [bug] `project.three.php` still requests videos from `http://localhost:8000/video/<name>.mp4` (ERR_BLOCKED_BY_ORB in the browser network log) — a path with no Kirby route; pre-existing (see Phase 5 backlog item on `videoPath => 'app/video/'`), reconfirmed while browser-testing the isphording world.

- Phase 5 (2026-09-16): [bug] `@barba/core`'s `barba.go()` throws `TypeError: Cannot read properties of undefined (reading 'isRunning')` when invoked programmatically while a transition is already in flight (reproduced repeatedly during browser QA of the readiness gate); real users double-clicking a nav link or clicking during a transition could hit the same path — consider guarding link/`go()` calls against an in-flight transition.
- Phase 5 (2026-09-16): [note] The reveal is driven by the GSAP `scaleX(0)` transform collapse, not by opacity; after a Barba swap the `.loadingScreen` can sit at `opacity:1; visibility:visible` and is only visually hidden because its `scaleX` is 0 (observed when `html.js-ready` was absent in a fresh isolated browser context). The coupling between `html.js-ready`/`is-transitioning` CSS and the GSAP transform is fragile — if the `animateLoaderOut` onComplete reset ever fails to run, the cover would remain over the page.
- Phase 5 (2026-09-16): [note] The production bundle strips all `console.*` (Terser), so `readme/BARBA_FIX_SUMMARY.md`'s "check the console logs" testing steps only apply to the Vite dev build; added a caveat to the doc but the step-by-step log expectations could be rewritten for the built site.

- Phase 7 (2026-09-16): [note] `app/assets/pdf/` is a gitignored, server-owned tree the plan did not enumerate; Phase 7 added `--exclude='/assets/pdf/'` to `scripts/deploy.sh` alongside fonts/three/content/video so `--delete` cannot wipe live PDFs from an incomplete clone.
- Phase 7 (2026-09-16): [note] The plan's preflight spec named `app/kirby/index.php` as the Composer marker, but Kirby 5's core package ships `bootstrap.php` (no `index.php`); the site entry point is `app/index.php`. The preflight checks `app/kirby/bootstrap.php` + `app/index.php` accordingly.
- Phase 7 (2026-09-16): [note] The `app/assets/bundle` preflight check runs after `npm run build` (which regenerates bundle), so in a full deploy it mainly backstops a failed/empty build; the `app/index.php` / `app/.htaccess` / `app/kirby/bootstrap.php` checks are what actually catch the incomplete-clone case. Consider a pre-build clone-integrity check if fresh-clone deploys become common.
- Phase 7 (2026-09-16): [note] The live-server dry-run (`bash scripts/deploy.sh --dry-run`) was intentionally NOT run in the unattended plan runner — it runs `composer install --no-dev` (mutating `app/vendor`) and opens an SSH connection to production, which the house rules gate behind human approval. The rsync filter and preflight guard were instead proven with a deterministic local rsync `--dry-run --delete` simulation using the byte-identical EXCLUDES array (zero deleting lines for server-owned trees; bundle + draco transfer) and by exercising the extracted preflight functions (abort on missing/empty bundle, warn on absent server-owned trees).

- Phase 9 (2026-09-16): [note] Phase 9 removed the `url` hardcode from `config.localhost.php` (inherited by `config.127.0.0.1.php`), the permanent fix the Phase 1 improvements entry called for — the dev server now infers its origin from the request instead of pinning `http://localhost:8000`, so `PORT=8011` (or any other free port) works without a per-session local edit.
- Phase 9 (2026-09-16): [note] smoke.sh's new font-asset list (`ASSET_PATHS`) is a fixed enumeration of the five current webfont files; adding a new font weight/family to `dev/css/templates/_typography.scss` requires manually adding its path to that list too, or the gate won't notice a future double-prefix regression on the new file.
- Phase 9 (2026-09-16): [bug] Hit and fixed a bash pitfall while writing the port-in-use check: `exec 3>&- 3<&-` in the outer shell (closing a file descriptor that was only ever opened inside a `(exec 3<>...)` subshell) is a redirection error that aborts a non-interactive bash script immediately, silently ignoring a trailing `|| true`. Worth remembering for any future `/dev/tcp` probe written the same way.
- Phase 9 (2026-09-16): [note] The pre-existing `project.three.php` `/video/` ORB-block bug (Phase 5/10 backlog) is untouched by this phase — smoke.sh's asset assertions only cover the home showreel and fonts/CSS, not the Three.js gallery video paths.
